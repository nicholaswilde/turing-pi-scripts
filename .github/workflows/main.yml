on:
  push:
    branches:
      - main

jobs:
  build-deb:
    runs-on: ubuntu-latest
    steps:
      - uses: kentik/pkg@v1
        with:
          name: turing-pi-scripts
          version: 0.1
          arch: armhf
          format: deb
          package: package.yml
